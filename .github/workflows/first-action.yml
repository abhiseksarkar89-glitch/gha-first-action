name: Push + Manual Workflow

on:
  push:
    branches:
      - main   # triggers on push to main
  workflow_dispatch   # allows manual trigger

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Workflow run 🚀"
